import { makeStyles } from '@mui/styles';
export const classes = makeStyles({
  container: {
    padding: 10,
    height: '90vh',
  },
  cardContainer: {
    padding: 20,
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    display: 'flex',
    flexFlow: 'column',
  },
});
